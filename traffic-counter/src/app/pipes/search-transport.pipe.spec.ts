import { SearchTransportPipe } from './search-transport.pipe';

describe('SearchTransportPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchTransportPipe();
    expect(pipe).toBeTruthy();
  });
});
