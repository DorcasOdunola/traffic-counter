import { SearchUnitPipe } from './search-unit.pipe';

describe('SearchUnitPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchUnitPipe();
    expect(pipe).toBeTruthy();
  });
});
